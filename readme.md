# Unicorn In The Shell

Setps to setup this blog locally:
* clone the repo.
* install the following gems:
  - jemoji
  - jekyll-seo-tag
  - jekyll-gist
  - jekyll-feed
  - minima

Then:

* jekyll serve
